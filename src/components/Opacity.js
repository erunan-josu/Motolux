import styled from 'styled-components'

const Opacity = () => {
  return <OpacityStyled />
}

const OpacityStyled = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.3);
`

export default Opacity
